[package]
name = "omnimemora-desktop-shell"
version = "1.0.0-beta.3"
description = "OmniMemora desktop beta shell"
authors = ["OmniMemora"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }

[features]
default = []
