# P0-3 验收模板（自动化 + 手工）

## 基本信息

- 日期：
- 执行人：
- 环境：
- Runtime 地址：
- OpenClaw 版本：

## 自动化结果

- 命令：`omnimemora validate openclaw --profile p0-3 --runs 3 --out ./artifacts/p0_3_report`
- JSON 报告路径：
- Markdown 报告路径：
- 自动化结论：PASS / FAIL

## 手工结果（三类任务）

### 连续问答型

- MCP Handshakes：
- Tool Invocations：
- memory.write：
- memory.search/context/recall：
- token_savings.total_saved_tokens：
- 后半段/前半段比值：
- 结论：PASS / FAIL

### 编辑迭代型

- MCP Handshakes：
- Tool Invocations：
- memory.write：
- memory.search/context/recall：
- token_savings.total_saved_tokens：
- 后半段/前半段比值：
- 结论：PASS / FAIL

### 项目执行型

- MCP Handshakes：
- Tool Invocations：
- memory.write：
- memory.search/context/recall：
- token_savings.total_saved_tokens：
- 后半段/前半段比值：
- 结论：PASS / FAIL

## 固定阈值复核

- 无 MCP 启动错误：PASS / FAIL
- 工具调用持续非 0：PASS / FAIL
- savings 持续增长：PASS / FAIL
- 后半段 >= 前半段 1.3x：PASS / FAIL
- 前半段 >= 300：PASS / FAIL
- search/context 调用差异 <= 30%：PASS / FAIL
- Dashboard 全程 `Memory Tools Active`：PASS / FAIL

## 最终结论

- 自动化：PASS / FAIL
- 手工：PASS / FAIL
- 放行：PASS / FAIL
- 备注：

