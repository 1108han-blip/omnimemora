# 文档对齐记录（2026-04-10）

## 背景

用户要求：在优化产品后，代码与产品系统文档必须同步对齐，且文档落在主仓库路径：
`E:\AI2\Vault\13_OmniMemora\OmniMemora`

## 本次代码侧变更（接入层）

仓库：`4_core/local-runtime`

1. OpenClaw attach 改为 MCP schema 合法写法：
   - 从根键 `omnimemora` 改为 `mcp.servers.omnimemora.url`
2. OpenClaw detach 增加双清理：
   - 清理 `mcp.servers.omnimemora`
   - 清理历史遗留根键 `omnimemora`
3. IsAttached 逻辑升级：
   - 优先识别 `mcp.servers.omnimemora`
   - 兼容识别 legacy 根键（仅用于迁移期）
4. Dashboard 连接态修正：
   - 无数据时不再一律提示“去连接”
   - 已连接时显示“等待首条 memory 活动”
   - 连接指令统一为 `omnimemora attach ...`（移除旧 `connect` 文案）
5. MCP 协议层打通（OpenClaw）：
   - 新增 SSE MCP 入口（`/`, `/sse`, `/mcp`, `/mcp/sse`）
   - 新增 MCP 消息入口（`/messages`, `/mcp/messages`）
   - 新增直连入口：`POST /mcp`（JSON-RPC，无需先走 SSE session）
   - 新增工具：`omnimemora_write_memory`、`omnimemora_search_memory`
   - 新增兼容别名：`memory.write` / `memory.store`、`memory.search`、`memory.context` / `memory.recall`
6. 搜索兼容修复：
   - 关键词含连字符时 FTS5 语法可能报错（如 `project-alpha`）
   - 增加自动回退 LIKE 查询，避免“写入成功但搜索失败”
7. Dashboard 状态口径收敛（避免误导）：
   - `Configured` = 仅配置已写入
   - `Connected` = 已观察到 MCP 握手
   - `Active` = 已观察到真实 memory tool 调用
   - 状态卡新增计数：`MCP Handshakes`、`Tool Invocations`

## 本次文档同步

1. 新增策略文档：
   - `7_docs/internal/phase4/plan/零维护安装与MCP自动适配方案_v1.md`
2. 新增协同执行规范：
   - `7_docs/internal/phase4/plan/多代理协同执行规范_v1.md`
2. 本文件作为变更-文档对齐凭证：
   - `7_docs/internal/phase4/plan/文档对齐记录_2026-04-10.md`

## 多代理执行证据（本轮）

1. OpenClaw：
   - `openclaw status` 已执行（输出含安全审计摘要）
   - `openclaw config validate` 通过
   - `openclaw agent --local --agent main` 已产出文档提纲建议
2. Claude Code：
   - 执行 OpenClaw attach 成功判定升级任务（写入+读回+validate）
3. Codex：
   - 复核测试、整合结果、并将文档同步到主仓库 `7_docs`

## 验证记录

1. `omnimemora detach openclaw` 后，`openclaw config validate` 通过。
2. `omnimemora attach openclaw` 后，配置包含：
   - `mcp.servers.omnimemora.url = "http://127.0.0.1:18011"`
3. `openclaw config validate` 通过，无未识别键报错。
4. P0 接入验收口径升级为四条硬门槛（必须同时满足）：
   - OpenClaw 启动不再报 MCP/SSE 404
   - OmniMemora MCP transport 可完成握手
   - OpenClaw 可列出并调用至少一个 memory 工具
   - Dashboard 出现真实 tool invocation，且出现非 0 usage（savings 非 0为加分但非唯一判据）

## 后续约束

从本记录起，凡是接入层变更（attach/detach/detect/verify），必须同日更新 `7_docs/internal/phase4/plan` 下对应文档，否则不得视为完成交付。
