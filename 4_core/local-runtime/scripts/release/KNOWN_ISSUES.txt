OMNIMEMORA CONTROLLED BETA KNOWN ISSUES
Package version: {{PACKAGE_VERSION}}

- This is a controlled desktop beta package, not a production release.
- macOS builds are ad-hoc signed and updater-signed, but not Apple Developer ID notarized; first launch may require manual System Settings approval.
- Normal downloadable app releases are blocked unless signed desktop updater artifacts are hosted and verified end-to-end.
- Local component updates are manifest-based and require explicit user confirmation.
- Failed component updates must roll back to the previous local component set.
- Cloud-enhanced candidate source remains optional; local operation is the primary path.
- Cloud policy candidates are not auto-promoted over local active policy.
- Some client integrations may still require manual attach/connect guidance; attached Claude-family launchers should not require manual JSON edits when 18011 is initially unavailable.
- When reporting problems, include: product version, platform, request_id, error_code, steps to reproduce, and screenshots when available.

Support
{{SUPPORT_EMAIL}}
